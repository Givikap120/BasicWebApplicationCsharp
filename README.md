Basic story-type app with JWT bearer token authification.
Files in the "Entities" folder are generated automatically using templates from the folder "CodeTemplates", which exist to rename classes by adding the suffix "Entity".
Script "EntityGenerate.ps1" is needed to be run in the Package Manager Console to generate Entity files from DB.
